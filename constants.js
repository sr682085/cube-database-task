const SORTING_ORDER = {
  DESC: -1,
  ASC: 1
}

const DONOR_DATASET_FILE = './dataset/Donors.csv'
const DONATION_DATASET_FILE = './dataset/Donations.csv'

const STATE_HEADING = 'State'
const DONATION_HEADING = 'Donation (In $)'

module.exports = {
  SORTING_ORDER,
  DONATION_DATASET_FILE,
  DONOR_DATASET_FILE,
  STATE_HEADING,
  DONATION_HEADING
}
